# CrickCoach Backend Dependencies
# Updated: 2026-02-20

# Core Flask and web framework
Flask>=2.3.0
Flask-CORS>=4.0.0
Werkzeug>=2.3.0

# Production WSGI Server
gunicorn>=21.2.0
gevent>=23.7.0

# Authentication and security
PyJWT>=2.8.0
bcrypt>=4.0.0

# Machine Learning and Computer Vision
torch>=2.0.0
torchvision>=0.15.0
tensorflow>=2.13.0
tensorflow-hub>=0.15.0
opencv-python>=4.8.0
pytorchvideo>=0.1.5

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Google Generative AI (Gemini)
google-generativeai>=0.3.0

# OpenAI API (if used)
openai>=1.0.0

# Google Drive download utility
gdown>=4.7.0

# Image processing
Pillow>=10.0.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
setuptools>=68.0.0

# Development and testing (optional)
pytest>=7.4.0
pytest-flask>=1.3.0

# Note: sqlite3 is part of Python standard library and doesn't need to be installed
# Note: Standard library modules (os, json, csv, re, datetime, logging, time, 
#       functools, glob, threading, uuid, smtplib, email, random, string, 
#       subprocess, shutil) are included with Python and don't need to be installed
